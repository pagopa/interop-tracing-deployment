name: "tracing-be-operations"

serviceAccount:
  roleArn: "arn:aws:iam::537124933279:role/tracing-be-operations-qa"

service:
  create: true
  portName: http
  containerPort: 8080

configmap:
  DB_SCHEMA_NAME: "qa_tracing"
  APPLICATION_NAME: "tracing-operations"
  S3_BUCKET_NAME: "interop-tracing-files-qa"

deployment:
  flywayInitContainer:
    create: true
    migrationsConfigmap: "common-flyway-tracing-schema"
    env:
      FLYWAY_CREATE_SCHEMAS: true
      FLYWAY_PLACEHOLDER_REPLACEMENT: true
      FLYWAY_PLACEHOLDERS_NAMESPACE: "qa"
      FLYWAY_URL: jdbc:postgresql://$(POSTGRES_HOST):$(POSTGRES_PORT)/$(POSTGRES_DB)
      FLYWAY_SCHEMAS: "qa_tracing"
    envFromConfigmaps:
      POSTGRES_HOST: "common-tracing-store.TRACING_STORE_DB_HOST_RW"
      POSTGRES_PORT: "common-tracing-store.TRACING_STORE_DB_PORT"
      POSTGRES_DB: "common-tracing-store.TRACING_STORE_DB_NAME"
    envFromSecrets:
      FLYWAY_USER: "tracing-store-flyway-user.username"
      FLYWAY_PASSWORD: "tracing-store-flyway-user.password"

  env:
    HOST: "0.0.0.0"
    PORT: 8080
  envFromConfigmaps:
    TRACING_STORE_DB_HOST: "common-tracing-store.TRACING_STORE_DB_HOST_RW"
    TRACING_STORE_DB_NAME: "common-tracing-store.TRACING_STORE_DB_NAME"
    TRACING_STORE_DB_PORT: "common-tracing-store.TRACING_STORE_DB_PORT"
    TRACING_STORE_DB_USE_SSL: "common-tracing-store.TRACING_STORE_DB_USE_SSL"
  envFromSecrets:
    TRACING_STORE_DB_USERNAME: "tracing-store-operations-user.username"
    TRACING_STORE_DB_PASSWORD: "tracing-store-operations-user.password"
  livenessProbe:
    httpGet:
      path: "/status"
      port: http
    periodSeconds: 5
    initialDelaySeconds: 15
  readinessProbe:
    httpGet:
      path: "/status"
      port: http
    periodSeconds: 5
    initialDelaySeconds: 15
